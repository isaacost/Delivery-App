import styled from 'styled-components';

const ContainerMyOrders = styled.div`
  #orders-container {
    display: flex;
    flex-flow: row wrap;
    gap: 24px;
    justify-content: center;
    align-items: center;
  }
`;

export default ContainerMyOrders;
